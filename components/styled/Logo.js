import styled from "styled-components";

const Logo = styled.div`
    display: flex;
    justify-content: space-around;
    margin-top: 50px;

    & div {
        width: 50px;
    }

    & div img {
        width: 100%;
    }
`;

export default Logo;
